start user and admin interaction with new repository

user side
--> show lend books on user dashboard and update it also as it is completed in admin panel work - DONE
--> show purchased book on user dashboard and update it also as it is completed in admin panel work - DONE

admin panel
--> Make filters work all tabs in admin panel
--> in book model add price option tooo and when fetching data fetch the price and fill price option



most important thing clean the code file by file once finished working



date function
 function formatLendDate(dateString) {
    const date = new Date(dateString);
    const day = date.getDate();
    const month = date.toLocaleString('en-US', { month: 'short' }).toUpperCase();
    const year = date.getFullYear();

    // Determine the suffix for the day
    const suffix = (day % 10 === 1 && day !== 11) ? "st" :
                   (day % 10 === 2 && day !== 12) ? "nd" :
                   (day % 10 === 3 && day !== 13) ? "rd" : "th";

    return `${day}${suffix} ${month}, ${year}`;
}
