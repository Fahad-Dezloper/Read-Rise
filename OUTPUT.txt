resolve all bugs


admin panel
--> when fetching book data show loading if availanle in dot colored [red, green, orange]
--> when click on return loading button and update it
--> sale tab add 2 features of discount and price like if mrp is 699 sold it 499 it automatically calculates the percenatage given off to the customer or how much money discount is given.
--> make loading component for the tabs and button when processing
--> shadcn alert
---> user tab subscription expiry filter


user dashboard
--> Update Books and User

most important thing clean the code file by file once finished working

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


date function
 function formatLendDate(dateString) {
    const date = new Date(dateString);
    const day = date.getDate();
    const month = date.toLocaleString('en-US', { month: 'short' }).toUpperCase();
    const year = date.getFullYear();

    // Determine the suffix for the day
    const suffix = (day % 10 === 1 && day !== 11) ? "st" :
                   (day % 10 === 2 && day !== 12) ? "nd" :
                   (day % 10 === 3 && day !== 13) ? "rd" : "th";

    return `${day}${suffix} ${month}, ${year}`;
}
