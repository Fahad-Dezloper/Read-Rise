/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", "m.media-amazon.com", "ik.imagekit.io", "www.midlandbookshop.com"],
  },
};

export default nextConfig;
